apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-go-config
  namespace: coffeeshop
data:
  PORT: "8080"
  KEYCLOAK_REALM: "coffeeshop"
  KEYCLOAK_BACKEND_CLIENT_ID: "coffeeshop-backend"
  CORS_ALLOWED_ORIGINS: "https://coffeeshop.example.com"
