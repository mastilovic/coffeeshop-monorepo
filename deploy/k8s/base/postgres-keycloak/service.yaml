apiVersion: v1
kind: Service
metadata:
  name: postgres-keycloak
  labels:
    app.kubernetes.io/name: postgres-keycloak
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: postgres-keycloak
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
