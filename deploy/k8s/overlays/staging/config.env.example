# CI/CD: generated by deploy workflow from GitHub Variables (see deploy/GITHUB_SETUP.md).
# Local: copy to config.env, set values, then run ./render-local.sh before kubectl apply.

APP_HOST=app.kafenerija.online
AUTH_HOST=auth.kafenerija.online
PUBLIC_SCHEME=http

POSTGRES_DB=coffeeshop
POSTGRES_USER=coffeeshop
KEYCLOAK_POSTGRES_DB=keycloak
KEYCLOAK_POSTGRES_USER=keycloak

KC_DB_URL=jdbc:postgresql://postgres-keycloak:5432/keycloak
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/coffeeshop

KEYCLOAK_REALM=coffeeshop
KEYCLOAK_BACKEND_CLIENT_ID=coffeeshop-backend
KEYCLOAK_PUBLIC_URL=http://auth.kafenerija.online
KEYCLOAK_JWT_ISSUER_URI=http://auth.kafenerija.online/realms/coffeeshop
CORS_ALLOWED_ORIGINS=http://app.kafenerija.online
