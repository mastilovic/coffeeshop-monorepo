# CI/CD: generated by deploy workflow from GitHub Secrets (STAGING_* names).
# Local: copy to secrets.env, then run ./render-local.sh before kubectl apply.

POSTGRES_PASSWORD=changeme
KEYCLOAK_POSTGRES_PASSWORD=changeme
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme
KEYCLOAK_BACKEND_CLIENT_SECRET=changeme
